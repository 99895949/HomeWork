server:
  port: 9000
  servlet:
    context-path: /park

spring:
  datasource:
#  必须指定时区
    url: jdbc:mysql://localhost:3306/park_sys?characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    show-sql: true
#    开启SpringBoot热部署
  devtools:
    remote:
      restart:
        enabled: true


